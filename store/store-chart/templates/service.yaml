apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-chart.fullname" . }}
  labels:
    {{- include "store-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "store-chart.selectorLabels" . | nindent 4 }}
